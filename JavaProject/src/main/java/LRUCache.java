import java.util.HashMap;
import java.util.Map;

/**
 * Created by SHILPESH on 11-Feb-17.
 * Problem
 * Implement a LRU cache. The LRU cache hold values of type V.
 * Each value is associated with a key of type K. The cache is of
 * finite size and when the cache grows past its size limit, it deletes
 * items that are least recently used
 */
public class LRUCache extends Map.Entry{
    @Override
    public Object getValue() {
        return null;
    }

    @Override
    public Object setValue(Object value) {
        return null;
    }

    @Override
    public boolean equals(Object o) {
        return false;
    }

    @Override
    public int hashCode() {
        return 0;
    }
}
